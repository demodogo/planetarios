import { MetodologiaPage } from '@/ui/generaciones/metodologia-page';
import type { Metadata } from 'next';

export const metadata: Metadata = {
	title: 'Metodología',
};

export default function Page() {
	return <MetodologiaPage />;
}
